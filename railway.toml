[build]
builder = "nixpacks"

[deploy]
startCommand = "python3 railway_legacy_redirect.py"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
